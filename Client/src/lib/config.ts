/**
 * Application configuration
 */
export const config = {
  api: {
    baseUrl: "https://api.crypto-pilot.dev",
  },
  auth: {
    // Auth related configuration can go here
    tokenStorageKey: "auth_token",
  }
};
